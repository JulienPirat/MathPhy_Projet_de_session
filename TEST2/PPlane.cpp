#include "PPlane.h"
#include "RigidBody.h"
#include <vector>
/*
std::vector<Vector3D> PPlane::getVertices()
{
    // Définition des coins du plan
    std::vector<Vector3D> vertices;

    vertices.push_back(Vector3D(RB->position.x - halfsizeX, RB->position.x - halfsizeY, 0));
    vertices.push_back(Vector3D(RB->position.x - halfsizeX, RB->position.x + halfsizeY, 0));
    vertices.push_back(Vector3D(RB->position.x + halfsizeX, RB->position.x - halfsizeY, 0));
    vertices.push_back(Vector3D(RB->position.x + halfsizeX, RB->position.x + halfsizeY, 0));



    return vertices;
}
*/